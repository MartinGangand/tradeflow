name: PyPi verify

on: [push, workflow_dispatch]

jobs:
  run-script:
    name: Check PyPi page

    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Run script
        run: python scripts/pypi-verify.py
